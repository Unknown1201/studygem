import React from 'react';

// DEPRECATED: This component is no longer in use.
// It has been replaced by the new theme-aware `ListItem.tsx` component
// to support the new light and dark mode design system.
const GlassListItem: React.FC = () => {
    return null;
};

export default GlassListItem;